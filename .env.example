# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=job_db

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Gollama LLM Backend
# Use http://host.docker.internal:8000/generate if running in Docker on Mac
GOLLAMA_URL=http://localhost:8000/generate